PORT=5000
# Neon database connection string
# IMPORTANT: Check Neon dashboard to ensure database is NOT paused
# If paused, click "Resume" and wait 30 seconds
# Get fresh connection string from Neon dashboard if issues persist
DATABASE_URL=postgresql://neondb_owner:npg_6GqQn9eatDSX@ep-morning-fire-ahgqddjf-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require&channel_binding=require
JWT_SECRET=your_super_secret_jwt_key_change_in_production_min_32_chars
JWT_EXPIRE=7d
NODE_ENV=development

# Super Admin seed (node scripts/seedSuperAdmin.js):
# Institution ID for Veman Academy is PRV-F-000018 (used in /venam/PRV-F-000018 and super-admin login).
# Optional overrides:
# SUPER_ADMIN_EMAIL=superadmin@pravidya.local
# SUPER_ADMIN_PASSWORD=SuperAdmin@123

FRONTEND_URL=http://localhost:3000
# Parent Feedback Form: link in email must point to where the form runs (port 5174 for local dev)
FEEDBACK_FORM_URL=http://localhost:5174

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=pravidya1234@gmail.com
SMTP_PASS=hucbhxlsbjiypbcj
SMTP_FROM="Pravidya <pravidya1234@gmail.com>"

GEMINI_API_KEY=AIzaSyAsRoZiX7YBl8sBj59tXAvbwOYKRyAAOC8
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
GEMINI_CHAT_MODEL=gemini-2.5-flash
INTELLIGENCE_MAX_OUTPUT_TOKENS=2048
# Optional: max tokens for Intelligence Chat answers (default 1024; increase if answers still cut off)
# INTELLIGENCE_MAX_OUTPUT_TOKENS=1024

# Google Meet / Calendar (counselor video sessions): OAuth + create Meet link
# Use your upgraded Google Cloud OAuth 2.0 credentials. In Google Cloud Console add this redirect URI: http://localhost:5000/api/counselor/calendar/callback

# Optional: frontend origin for OAuth redirect after generating Meet link (default http://localhost:3000)
# FRONTEND_ORIGIN=http://localhost:3000


GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret